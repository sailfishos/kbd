diff --git a/src/loadkeys.c b/src/loadkeys.c
index f3a4560..f0fbf85 100644
--- a/src/loadkeys.c
+++ b/src/loadkeys.c
@@ -2991,7 +2991,7 @@ int main(int argc, char *argv[])
 		}
 
  gotf:
-		if (!quiet && !optm)
+		if (verbose && !optm)
 			fprintf(stdout, _("Loading %s\n"), pathname);
 
 		stack_push(f, 0, pathname);
